# Simulation and integration settings
simulation:
  t_start_s: 0.0
  t_end_s: 3000.0
  max_step_s: 0.5

  constraints:
    max_q_Pa: 35_000.0
    fairing_deploy_altitude_m: 120_000.0
    min_staging_altitude_m: 60_000.0
